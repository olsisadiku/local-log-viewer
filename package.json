{
  "name": "docker-log-viewer",
  "version": "0.1.0",
  "private": true,
  "description": "A lightweight, real-time log viewer for Docker Compose with ELK-like features",
  "author": "",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @docker-log-viewer/cli dev",
    "build": "pnpm --filter @docker-log-viewer/shared build && pnpm --filter @docker-log-viewer/web build && pnpm --filter @docker-log-viewer/cli build && cp -r packages/web/dist packages/cli/dist/public",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint packages --ext .ts,.tsx",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "eslint": "^8.55.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
